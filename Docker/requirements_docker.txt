# ===================================
# MPSA项目 - Docker专用依赖清单
# 基础镜像: pytorch/pytorch:2.0.0-cuda11.8-cudnn8-runtime
# 已包含: torch==2.0.0, torchvision==0.15.1, numpy
# ===================================

# 核心依赖 - 训练和推理必需
# ===================================
# 视觉Transformer库
timm==1.0.19

# 张量操作
einops==0.8.1

# 科学计算
scipy==1.10.1

# 数据处理
pandas==2.0.3

# 图像处理
opencv-python==4.12.0.88
opencv-contrib-python==4.12.0.88
Pillow==9.4.0

# 可视化
matplotlib==3.7.1

# 配置管理
yacs==0.1.8

# 工具库
tqdm
PyYAML==6.0

# 可选依赖 - 仅用于可视化脚本
# ===================================
# 注意：训练和推理不需要这些包，但为完整性保留
pytorch-grad-cam>=1.3.0
scikit-learn>=1.0.0
